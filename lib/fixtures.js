Game = new Meteor.Collection("game");

/*if(Meteor.users.find().count() === 0){
	var wins = 4;
	var losses = 2;
	var total = wins + losses;
	var pct = (wins/total);
	pct *=100;
	Accounts.createUser({
		email:"splash@GSW.com",
		password:"password",
		profile:{
			nickname:"Stephen Curry",
			wins:wins,
			losses:losses,
			pct:pct
		}
	});

	wins = 5;
	losses = 1;
	total = wins + losses;
	pct = (wins/total);
	pct *=100;
	Accounts.createUser({
		email:"starkt@avengers.com",
		password:"password",
		profile:{
			nickname:"Tony Stark",
			wins:wins,
			losses:losses,
			pct:pct
		}
	});

	wins = 2;
	losses = 4;
	total = wins + losses;
	pct = (wins/total);
	pct *=100;
	Accounts.createUser({
		email:"beatles@music.com",
		password:"password",
		profile:{
			nickname:"John Lennon",
			wins:wins,
			losses:losses,
			pct:pct
		}
	});

	wins = 4;
	losses = 0;
	total = wins + losses;
	pct = (wins/total);
	pct *=100;
	Accounts.createUser({
		email:"nsync@music.com",
		password:"password",
		profile:{
			nickname:"Justin Timberlake",
			wins:wins,
			losses:losses,
			pct:pct
		}
	});
} */
